# Sprint Zero Notes

- Framework choice: Next.js 14 App Router with TypeScript strict mode.
- Initial shell UI: landing dashboard, dashboard view, and dynamic note room placeholder routes.
- Styling approach: plain global CSS for the first scaffold so the app is runnable before the design system is added.
- Follow-up: replace the placeholder editor canvas with the real collaborative CRDT editor and note data layer.